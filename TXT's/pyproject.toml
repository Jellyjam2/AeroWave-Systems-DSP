[build-system]
requires = ["maturin>=1.0,<2.0"]
build-backend = "maturin"

[project]
name = "titan_forge"
version = "0.1.0"
description = "🜏 LUMINA: STATEFUL SOVEREIGN VAULT"
requires-python = ">=3.8"
# Only essential pillars remain
dependencies = [
    "python-sat",
]

[tool.maturin]
features = ["pyo3/extension-module"]
